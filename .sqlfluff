[sqlfluff]
dialect = snowflake
templater = dbt
max_line_length = 120
# Ignore the specific rules causing your test failures
exclude_rules = LT12, CP01, RF04, ST06, LT08

[sqlfluff:templater:dbt]
project_dir = banking_dbt
profiles_dir = ~/.dbt